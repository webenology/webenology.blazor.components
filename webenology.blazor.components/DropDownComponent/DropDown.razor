@namespace webenology.blazor.components

<div>
    <select class="@Css()" value="@Value" @onchange="onChangeValue">
        @if (ShowEmpty)
        {
            <option value="@EmptyValue">@EmptyText</option>
        }
        @if (Items != null)
        {
            foreach (var i in Items)
            {
                <option value="@i.Value">@i.Key</option>
            }
        }
    </select>
    @if (IsError)
    {
        <label class="error">
            @ErrorMessage
        </label>
    }
</div>

